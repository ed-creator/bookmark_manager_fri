<h1> Links </h1>

<form action="/links/new">
  <input type="submit" value="Save new link" />
</form>

<ul id='links'>
  <% @links.each do |link| %>
    <li>
      Title: <%= link.title %>
      URL: <%= link.url %>
    </li>
  <% end %>
  <% @tags.each do |tag| %>
    <li>
      Tag: <%= tag.tag_name %>
    </li>
  <% end %>
</u1>
